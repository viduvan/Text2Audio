# ============================================
# Text2Audio - Story to YouTube Video Pipeline
# ============================================

# --- Core ---
pyyaml>=6.0
tqdm>=4.65
Pillow>=10.0
aiofiles>=23.0
python-dotenv>=1.0              # Load .env file

# --- Text Rewriter (N1) ---
google-genai>=1.0              # Gemini API for text rewriting
# ollama                       # Optional: pip install ollama (for local LLM)

# --- TTS (Text-to-Speech, N3) ---
edge-tts>=6.1
soundfile>=0.12                # Audio duration measurement

# --- Image Generation (SDXL + LCM LoRA, N4) ---
diffusers>=0.31.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
peft>=0.11.0                   # LoRA adapter support

# --- Video Processing (N5, N7, N8) ---
moviepy>=2.0
imageio[ffmpeg]>=2.34

# --- Web UI ---
gradio>=5.0

# --- PyTorch (install separately with CUDA) ---
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# OR for cu121:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
